const SG=[{t:'n',v:1,c:21,co:'#3b82f6',l:'1x',i:'\u{0031}'},{t:'n',v:2,c:13,co:'#10b981',l:'2x',i:'\u{0032}'},{t:'n',v:5,c:7,co:'#fbbf24',l:'5x',i:'\u{0035}'},{t:'n',v:10,c:4,co:'#ec4899',l:'10x',i:'\u{0031}\u{0030}'},{t:'b',v:2,c:2,co:'#a855f7',l:'COIN FLIP',bt:'cf',i:'\u{1FAC8}'},{t:'b',v:2,c:2,co:'#f97316',l:'CASH HUNT',bt:'ch',i:'\u{1F3AF}'},{t:'b',v:2,c:2,co:'#ef4444',l:'PACHINKO',bt:'pa',i:'\u{1FA9F}'},{t:'b',v:2,c:1,co:'#6ee7b7',l:'CRAZY TIME',bt:'ct',i:'\u{1F3AE}'}];
const TOT=SG.reduce((s,g)=>s+g.c,0);let bal=10000,bets={},spin=false,$=id=>document.getElementById(id),rIdx=0;function drawWheel(ctx,w,h,ang){const cx=w/2,cy=h/2,r=Math.min(cx,cy)*0.9;ctx.clearRect(0,0,w,h);ctx.save();ctx.shadowColor='#a855f7';ctx.shadowBlur=20;ctx.beginPath();ctx.arc(cx,cy,r+10,0,Math.PI*2);ctx.strokeStyle='rgba(168,85,247,.4)';ctx.lineWidth=6;ctx.stroke();ctx.restore();let sa=ang;for(const sg of SG){const sw=(sg.c/TOT)*Math.PI*2;ctx.beginPath();ctx.moveTo(cx,cy);ctx.arc(cx,cy,r-10,sa,sa+sw);ctx.closePath();ctx.fillStyle=sg.co;ctx.fill();ctx.strokeStyle='rgba(0,0,0,.3)';ctx.lineWidth=1.5;ctx.stroke();const ma=sa+sw/2;ctx.fillStyle='#fff';ctx.font=old px Inter,sans-serif;ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(sg.l,cx+Math.cos(ma)*r*0.65,cy+Math.sin(ma)*r*0.65);sa+=sw}ctx.beginPath();ctx.arc(cx,cy,r*0.2,0,Math.PI*2);const g=ctx.createRadialGradient(cx,cy,0,cx,cy,r*0.2);g.addColorStop(0,'#1a0530');g.addColorStop(1,'#2d1045');ctx.fillStyle=g;ctx.fill();ctx.strokeStyle='#a855f7';ctx.lineWidth=3;ctx.stroke();ctx.fillStyle='#fbbf24';ctx.font='bold 16px Poppins,sans-serif';ctx.fillText('\u{1F3AE}',cx,cy)}
function buildBet(){const co=bo;if(!co)return;co.innerHTML='';SG.forEach((sg,i)=>{const b=document.createElement('div');b.className='bet-odd-btn';b.dataset.idx=i;b.style.borderColor=sg.co;b.innerHTML=<span class="ic"></span><span class="lb"></span><span class="od" style="color:">@x</span>;b.addEventListener('click',()=>{if(spin)return;const a=Math.min(5000,Math.max(1,parseInt(betInput.value)||10));if(bets[i]!==undefined){delete bets[i];b.classList.remove('sel')}else{bets[i]=a;b.classList.add('sel')}td.textContent=Object.values(bets).reduce((s,v)=>s+v,0)});co.appendChild(b)})}