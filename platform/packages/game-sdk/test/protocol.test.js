import test from'node:test';import assert from'node:assert/strict';import{GAME_MESSAGES,isMessage,validBet,validBonusSpin,validMinesStart,validMinesReveal,validMinesCashout,validHiloStart,validHiloGuess,validHiloCashout,validBjStart,validBjAction,validChStart,validChAction,validVpStart,validVpDraw}from'../src/protocol.js';
test('accepts only known structured messages',()=>{assert.equal(isMessage({type:'GAME_READY'},GAME_MESSAGES),true);assert.equal(isMessage({type:'EVIL'},GAME_MESSAGES),false);assert.equal(isMessage(null,GAME_MESSAGES),false)});
test('validates integer virtual-credit bets and identifiers',()=>{assert.equal(validBet({amount:100,gameId:'slots-classic',roundId:'round_123456'}),true);assert.equal(validBet({amount:-1,gameId:'slots-classic',roundId:'round_123456'}),false);assert.equal(validBet({amount:1.5,gameId:'slots-classic',roundId:'round_123456'}),false)});
test('accepts bets whose optional choice arrives as an explicit undefined',()=>{assert.equal(validBet({amount:100,gameId:'wheel',roundId:'wheel_12345678',choice:undefined}),true);assert.equal(validBet({amount:100,gameId:'roulette',roundId:'round_12345678',choice:{type:'red'}}),true);assert.equal(validBet({amount:100,gameId:'roulette',roundId:'round_12345678',choice:[1,2]}),false);assert.equal(validBet({amount:100,gameId:'roulette',roundId:'round_12345678',choice:'red'}),false)});
test('validates opaque bonus sessions and idempotency round ids',()=>{assert.equal(validBonusSpin({gameId:'slot-original-001',sessionId:'bs_round_base_001',roundId:'round_free_001'}),true);assert.equal(validBonusSpin({gameId:'slot-original-001',sessionId:'forged',roundId:'round_free_001'}),false)});
test('reconnect protocol accepts only declared round status messages',()=>{assert.equal(isMessage({type:'ROUND_STATUS_REQUEST',payload:{roundId:'round_123456'}},GAME_MESSAGES),true);assert.equal(isMessage({type:'ROUND_STATUS',payload:{roundId:'round_123456',status:'open'}},new Set(['ROUND_STATUS'])),true)});
test('mines protocol messages are registered',()=>{for(const t of['MINES_START','MINES_REVEAL','MINES_CASHOUT'])assert.equal(GAME_MESSAGES.has(t),true)});
test('validates mines start bounds',()=>{assert.equal(validMinesStart({bet:100,mines:3}),true);assert.equal(validMinesStart({bet:100,mines:0}),false);assert.equal(validMinesStart({bet:100,mines:25}),false);assert.equal(validMinesStart({bet:0,mines:3}),false);assert.equal(validMinesStart({bet:1.5,mines:3}),false)});
test('validates mines session actions',()=>{const id='mines_'+'a1b2c3d4-e5f6-7890-abcd-ef1234567890';assert.equal(validMinesReveal({sessionId:id,tile:0}),true);assert.equal(validMinesReveal({sessionId:id,tile:25}),false);assert.equal(validMinesReveal({sessionId:'forged',tile:0}),false);assert.equal(validMinesCashout({sessionId:id}),true);assert.equal(validMinesCashout({sessionId:'x'}),false)});
test('validates hilo start/guess/cashout',()=>{const id='hilo_'+'a1b2c3d4-e5f6-7890-abcd-ef1234567890';assert.equal(validHiloStart({bet:100}),true);assert.equal(validHiloStart({bet:0}),false);assert.equal(validHiloGuess({sessionId:id,direction:'hi'}),true);assert.equal(validHiloGuess({sessionId:id,direction:'up'}),false);assert.equal(validHiloGuess({sessionId:'x',direction:'lo'}),false);assert.equal(validHiloCashout({sessionId:id}),true)});
test('validates blackjack start/action',()=>{const id='bj_'+'a1b2c3d4-e5f6-7890-abcd-ef1234567890';assert.equal(validBjStart({bet:100}),true);assert.equal(validBjStart({bet:0}),false);assert.equal(validBjAction({sessionId:id,move:'hit'}),true);assert.equal(validBjAction({sessionId:id,move:'split'}),false);assert.equal(validBjAction({sessionId:'x',move:'stand'}),false)});
test('validates holdem start/action',()=>{const id='holdem_'+'a1b2c3d4-e5f6-7890-abcd-ef1234567890';assert.equal(validChStart({bet:100}),true);assert.equal(validChStart({bet:0}),false);assert.equal(validChAction({sessionId:id,move:'call'}),true);assert.equal(validChAction({sessionId:id,move:'fold'}),true);assert.equal(validChAction({sessionId:id,move:'raise'}),false);assert.equal(validChAction({sessionId:'x',move:'call'}),false)});
test('validates videopoker start/draw',()=>{const id='vp_'+'a1b2c3d4-e5f6-7890-abcd-ef1234567890';assert.equal(validVpStart({bet:100}),true);assert.equal(validVpDraw({sessionId:id,hold:[0,2,4]}),true);assert.equal(validVpDraw({sessionId:id,hold:[]}),true);assert.equal(validVpDraw({sessionId:id,hold:[0,0]}),false);assert.equal(validVpDraw({sessionId:id,hold:[5]}),false);assert.equal(validVpDraw({sessionId:'x',hold:[0]}),false)});
