import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('80 cells and max 10 selections',async()=>{const s=await readFile(new URL('../game.js',import.meta.url),'utf8');assert.match(s,/numbersTotal:\s*80/);assert.match(s,/maxPicks:\s*10/);assert.match(s,/selected\.size\s*<\s*CONFIG\.maxPicks/);assert.match(s,/sdk\.settle/)});
